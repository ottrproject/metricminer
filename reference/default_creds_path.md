# Default Credentials path

Default Credentials path

## Usage

``` r
default_creds_path(app_name)
```

## Arguments

- app_name:

  What app set up are you looking for? Supported apps are 'google'
  'calendly' and 'github' Get file path to an default credentials RDS
