# App Set Up

This is a function that sets up the app. It's generally called by
another function

## Usage

``` r
app_set_up(app_name = "google")
```

## Arguments

- app_name:

  app would you like to authorize? Supported apps are 'google'
  'calendly' and 'github'
